import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#F03B7A',
    },
    secondary: {
      main: '#FAFAFB',
    },
    background: {
      default: '#14163E',
    },
    text: {
      primary: '#9a9dae',
      secondary: '#AEB0BF',
    },
  },
});

export default theme;
